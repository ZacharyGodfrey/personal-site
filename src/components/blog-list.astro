---

const allPosts = Object.values(import.meta.glob('../pages/blog/**/*.mdx', { eager: true }));

---

<ul>
  {allPosts.map((post: any) => (
    <li>
      <a href={post.url}>{post.frontmatter.title}</a>
    </li>
  ))}
</ul>